version: 2
jobs:
  build:
    docker:
    - image: circleci/php:7.1-jessie-node-browsers
  working_directory: ~/var/www/test_ci
  deploy_stg:
    machine:
      enabled: true
    working_directory: ~/var/www/test_ci
    steps:
      - run:
          name: Deploy via SSH
          command: ssh vagrant@192.168.33.15 'git pull'
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: STG
    
    
